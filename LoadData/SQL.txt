USE [Test2]
GO

/****** Object: Table [dbo].[NODES] Script Date: 14/06/2016 14:07:28 ******/
SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TABLE [dbo].[NODES] (
    [NODE_ID]    INT          NOT NULL,
    [NODE_LABEL] VARCHAR (50) NOT NULL
);




CREATE TABLE [dbo].[NODE_LINKS]
(
	[NODE_LINKS_ID] INT NOT NULL PRIMARY KEY, 
    [NODE_ID] INT NOT NULL, 
    [ADJACENT_NODE_ID] INT NOT NULL, 
    CONSTRAINT [FK_NODE_LINKS_NODES] FOREIGN KEY ([NODE_ID]) REFERENCES [NODES]([NODE_ID]),
	CONSTRAINT [FK_NODE_LINKS_ADJ_NODES] FOREIGN KEY ([ADJACENT_NODE_ID]) REFERENCES [NODES]([NODE_ID])
)
